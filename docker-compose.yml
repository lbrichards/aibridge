version: '3.8'
services:
  command-share:
    build: .
    ports:
      - "51753:51753"
    restart: unless-stopped
    environment:
      - REDIS_HOST=host.docker.internal
    extra_hosts:
      - "host.docker.internal:host-gateway"
